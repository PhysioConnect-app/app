/// Width (in logical pixels) below which the app switches from its desktop
/// layout to the mobile layout. This is purely a function of the available
/// width — desktop layouts also run in narrow browser windows, and mobile
/// layouts can run on a wide-screen device in portrait split view, etc.
const double kMobileBreakpoint = 600;
